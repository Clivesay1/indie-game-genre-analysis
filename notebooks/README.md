# Notebook Guide

Use `indie_game_genre_analysis.ipynb` as the supported, end-to-end analysis entry point. It loads the committed Steam genre snapshot, performs exploratory and opportunity-score analysis, and generates the project outputs.

The repository previously contained empty phase-notebook placeholders. They have been removed to avoid presenting invalid notebook files as runnable analysis steps.
